# import cv2


# face_cascade = cv2.CascadeClassifier('haarcascade_frontalface_default.xml')
# img = cc_image[-2][:, :]
# face = face_cascade.detectMultiScale(img, 1.3, 5)
# for (x, y, w, h) in face:
#     img = img[x:x + w, y:y + h]

# return all_processed_faces
